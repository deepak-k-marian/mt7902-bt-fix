PACKAGE_NAME="mt7902-bt"
PACKAGE_VERSION="1.0.0"

MAKE[0]="make -C /lib/modules/${kernelver}/build M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build/bluetooth modules"
CLEAN="make -C /lib/modules/${kernelver}/build M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build/bluetooth clean"

BUILT_MODULE_NAME[0]="btmtk"
BUILT_MODULE_LOCATION[0]="bluetooth"
DEST_MODULE_LOCATION[0]="/updates/dkms"

BUILT_MODULE_NAME[1]="btusb"
BUILT_MODULE_LOCATION[1]="bluetooth"
DEST_MODULE_LOCATION[1]="/updates/dkms"

AUTOINSTALL="yes"
